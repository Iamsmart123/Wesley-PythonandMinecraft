from mcpi.minecraft import Minecraft as mcs
mc = mcs.create()
x,y,z=mc.player.getTilePos()
number=10
renumber = 0
for i in range(number):
    for j in range(10)
    mc.spawnEntity(x,y,z,60)
    renumber = rennumber + 1
    mc.postToChat(str(i+1)+'隻魚 ')
                  
              